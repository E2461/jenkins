pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Clarusway_Way MENEME to Reinvent Yourself"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using DENEME dfdsfshell in the Jenkinsfile'
            }
        }
    }
}
